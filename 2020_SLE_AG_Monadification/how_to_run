
#How I ran it to get the test files available:

sudo docker run -i -t -v /home/tux/research/melt-artifacts/2020_SLE_AG_Monadification/examples/:/root/test-files/ melt-umn/implicit-monads

#What this is:
#  sudo                          docker is only accessible as root unless you make a special group and whatever
#  docker run                    run a docker file
#  -i                            run it interactively
#  -t                            tty
#  -v local-dir/:docker-dir/     make the local directory available at the docker directory
#  dockerfile                    the tag of the docker file to run


